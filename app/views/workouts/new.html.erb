<% provide(:title, 'New Workout') %>

<h1>New Workout</h1>

<div class="row">
	<aside class="span4">
		<section>
			<%= render 'shared/user_info' %>
		</section>
		<section>
			<%= render 'shared/workout_form' %>
		</section>
	</aside>
</div>  
