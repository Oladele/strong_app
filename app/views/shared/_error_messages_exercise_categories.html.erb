<% if @exercise_category.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@exercise_category.errors.count, "error") %>.
    </div>
    <ul>
    <% @exercise_category.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>